
jar {
    enabled = false
}

publishMavenPublicationToMavenLocal.onlyIf {  false  }

subprojects {
    publishing {
        publications {
            maven(MavenPublication) {
                artifactId "jarvis-deployment-${project.name}"
            }
        }
    }
}
